server {
    listen 8090;
    server_name SITENAME;

    location /static {
        alias /home/ubuntu/viberr/Viberr;
    }

    location / {
        proxy_set_header HOST $host;
        proxy_pass http://unix:/tmp/SITENAME.socket;
    }
}

